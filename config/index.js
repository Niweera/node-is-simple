const {
  SERVER_CERT,
  SERVER_KEY,
  HTTP_PORT,
  HTTPS_PORT,
  MONGO_URI,
  JWT_SECRET,
  SIGN_ALGORITHM,
  TOKEN_ISSUER,
  JWT_EXPIRES_IN,
  VERIFY_ALGORITHM
} = process.env;

module.exports = {
  SERVER_CERT,
  SERVER_KEY,
  HTTP_PORT,
  HTTPS_PORT,
  MONGO_URI,
  JWT_SECRET,
  SIGN_ALGORITHM,
  TOKEN_ISSUER,
  JWT_EXPIRES_IN,
  VERIFY_ALGORITHM
};
